# 🎮 خطة تنفيذ الألعاب التعليمية (Gamification Plan)

## 🎯 الهدف
دمج الألعاب التعليمية التفاعلية (مثل السحب والإفلات، المطابقة) كجزء من المسار التعليمي، حيث يتم التعامل مع "اللعبة" كنوع خاص من "الدروس".

---

## 📅 المرحلة 1: قاعدة البيانات (Database Schema)
**الهدف:** تهيئة الجدول `Lesson` لدعم أنواع محتوى مختلفة.

1.  **تعديل ملف `prisma/schema.prisma`:**
    *   إضافة حقل `contentType` إلى جدول `Lesson`.
    *   القيم المقترحة: `ARTICLE` (افتراضي)، `GAME`، `VIDEO`.
    *   إضافة حقل `gameConfig` (نوع `Json?`) لتخزين إعدادات اللعبة (مثل البيانات التي سيتم ترتيبها).

```prisma
model Lesson {
  // ... الحقول الحالية
  contentType String  @default("ARTICLE") @db.VarChar(20) // ARTICLE, GAME, VIDEO
  gameConfig  Json?   @map("game_config") // إعدادات اللعبة إذا كان النوع GAME
}
```

2.  **تنفيذ التغييرات:**
    *   تشغيل `npx prisma generate`.
    *   تشغيل `npm run db:push`.

---

## 📅 المرحلة 2: الواجهة الخلفية (Backend API)
**الهدف:** تحديث نقاط النهاية (Endpoints) للتعامل مع البيانات الجديدة.

1.  **تحديث `src/app/api/lessons/route.ts` (POST):**
    *   استقبال `contentType` و `gameConfig` من الطلب.
    *   تخزينها في قاعدة البيانات عند إنشاء الدرس.

2.  **تحديث `src/app/api/lessons/[id]/route.ts` (PUT/PATCH):**
    *   السماح بتحديث `contentType` و `gameConfig`.

---

## 📅 المرحلة 3: مكونات الواجهة (Frontend Components)
**الهدف:** بناء محرك الألعاب ومكونات العرض.

1.  **تثبيت المكتبات اللازمة:**
    *   `dnd-kit` (للسحب والإفلات) أو استخدام HTML5 Drag & Drop API لتقليل الاعتماديات.

2.  **إنشاء مجلد `src/components/games`:**
    *   `GameRenderer.tsx`: المكون الرئيسي الذي يقرر أي لعبة يعرض بناءً على الإعدادات.
    *   `SortingGame.tsx`: لعبة ترتيب العناصر (تصاعدي/تنازلي).
    *   `MatchingGame.tsx`: لعبة مطابقة الأزواج (مثلاً: الكسر مع العدد العشري).

3.  **تصميم `SortingGame`:**
    *   يستقبل `items` (قائمة عناصر) و `correctOrder` (الترتيب الصحيح).
    *   واجهة تفاعلية تسمح للطالب بسحب العناصر وترتيبها.
    *   زر "تحقق" لعرض النتيجة فورياً.

---

## 📅 المرحلة 4: إدارة المحتوى (Teacher Dashboard)
**الهدف:** تمكين المعلم من إنشاء درس من نوع "لعبة".

1.  **تحديث صفحة إنشاء/تعديل الدرس (`src/app/(main)/dashboard/teacher/lessons/create/page.tsx`):**
    *   إضافة خيار "نوع الدرس" (مقال / لعبة).
    *   إذا اختار "لعبة"، يظهر نموذج إعداد اللعبة بدلاً من المحرر النصي.
    *   **نموذج إعداد اللعبة:**
        *   اختيار نوع اللعبة (ترتيب، مطابقة).
        *   إدخال البيانات (مثلاً: قائمة الأعداد لترتيبها).

---

## 📅 المرحلة 5: عرض الدرس للطالب (Student View)
**الهدف:** عرض اللعبة بدلاً من النص عندما يكون الدرس من نوع لعبة.

1.  **تحديث صفحة عرض الدرس (`src/app/(main)/lessons/[id]/page.tsx`):**
    *   التحقق من `lesson.contentType`.
    *   إذا كان `ARTICLE`: عرض المحتوى النصي (كما هو حالياً).
    *   إذا كان `GAME`: استدعاء `<GameRenderer config={lesson.gameConfig} />`.

---

## 📅 المرحلة 6: بيانات تجريبية (Seeding)
**الهدف:** إضافة درس تجريبي لاختبار الميزة.

1.  **إنشاء سكربت `prisma/seed-game-lesson.ts`:**
    *   إضافة درس جديد بعنوان "لعبة ترتيب الأعداد العشرية".
    *   ضبط `contentType` على `GAME`.
    *   ضبط `gameConfig` ببيانات تجريبية (أعداد عشرية عشوائية).

---

## ✅ معايير النجاح
1.  المعلم يستطيع إنشاء درس واختيار نوع "لعبة".
2.  الطالب يرى اللعبة ويتفاعل معها (سحب وإفلات).
3.  النظام يصحح إجابة الطالب فورياً داخل اللعبة.
4.  عدم تأثر الدروس النصية الحالية بالتغييرات.