--- /dev/null
+++ /home/yazan/Documents/smartedu/plan.md
@@ -0,0 +1,96 @@
+# 🚀 خطة نشر التحديثات الجذرية على Railway (مع حماية البيانات)
+
+بما أننا قمنا بتغييرات كبيرة على هيكلية قاعدة البيانات (Schema) والملفات، فإن النشر المباشر قد يؤدي إلى توقف الموقع أو فقدان البيانات إذا لم يتم بحذر. هذه الخطة تضمن انتقالاً سلساً.
+
+---
+ :

 
 ## 🛑 المرحلة 1: الأمان والنسخ الاحتياطي (الأهم)

 بما أن النسخ الاحتياطي التلقائي في Railway مدفوع (Pro Plan)، سنعتمد على أدواتنا الخاصة وتصدير البيانات يدوياً
 
-قبل رفع أي كود، يجب تأمين البيانات الحالية الموجودة على Railway.
+بما أن النسخ الاحتياطي التلقائي في Railway مدفوع (Pro Plan)، سنعتمد على أدواتنا الخاصة وتصدير البيانات يدوياً:
 
+1.  **الخيار الأول: عبر التطبيق (نظام النسخ الاحتياطي الخاص بنا):**
+    *   سجل الدخول كمدير (Directeur) على الموقع الحالي (قبل التحديث).
+    *   اذهب إلى صفحة النسخ الاحتياطي: `/dashboard/directeur/backup`.
+    *   قم بإنشاء نسخة احتياطية جديدة (Export).
+    *   **هام جداً:** قم بتحميل ملف النسخة الاحتياطية (Download) إلى جهازك المحلي فوراً. لا تعتمد على بقائه في السيرفر لأن Railway يقوم بمسح الملفات المحلية عند إعادة النشر.
+
+2.  **الخيار الثاني (الأكثر أماناً للمطورين): تصدير قاعدة البيانات يدوياً:**
+    *   استخدم برنامج إدارة قواعد بيانات مثل **DBeaver** أو **MySQL Workbench** أو **TablePlus**.
+    *   استخدم بيانات الاتصال الموجودة في Railway (تبويب Variables أو Connect).
+    *   قم بعمل **SQL Dump** أو **Export** لجميع الجداول وحفظ الملف على جهازك (`smartedu_backup_date.sql`).
+    *   هذا يضمن لك نسخة كاملة يمكنك استعادتها في أي وقت بغض النظر عن حالة التطبيق.
 
 ---







+## ⚙️ المرحلة 2: إعداد متغيرات البيئة (Environment Variables)
+
+لقد أضفنا ميزات جديدة تعتمد على Google Drive، لذا يجب إضافة المفاتيح الخاصة بها في Railway قبل النشر وإلا سيفشل البناء (Build) أو التشغيل.
+
+1.  اذهب إلى تبويب **Variables** في خدمة التطبيق (Next.js) على Railway.
+2.  أضف المتغيرات التالية (انسخ القيم من ملف `.env` المحلي لديك):
+    *   `GOOGLE_CLIENT_EMAIL`
+    *   `GOOGLE_PRIVATE_KEY` (تأكد من نسخ المفتاح كاملاً).
+    *   `GOOGLE_DRIVE_ROOT_FOLDER_ID`
+    *   `NEXTAUTH_SECRET` (تأكد من وجوده).
+    *   `NEXTAUTH_URL` (يجب أن يكون رابط الموقع على Railway، مثل `https://smartedu-production.up.railway.app`).
+
+---
+
+## 🛠️ المرحلة 3: تحضير ملفات التهجير (Migrations) محلياً
+
+بما أن التغييرات "جذرية"، لا يجب الاعتماد على `prisma db push` فقط، بل يجب إنشاء ملف تهجير (Migration File) لفحص ما سيحدث للبيانات.
+
+1.  **في جهازك المحلي (Local Terminal):**
+    قم بتشغيل الأمر التالي لإنشاء ملف التهجير دون تطبيقه فوراً:
+    ```bash
+    npx prisma migrate dev --create-only --name major_update_drive_and_schema
+    ```
+
+2.  **فحص ملف التهجير:**
+    *   اذهب إلى المجلد `prisma/migrations/202xxxx_major_update.../migration.sql`.
+    *   **اقرأ الملف بعناية:** ابحث عن أي جملة تبدأ بـ `DROP TABLE` أو `DROP COLUMN`.
+    *   ⚠️ **تحذير:** إذا وجدت `DROP COLUMN` لعمود يحتوي على بيانات مهمة، يجب عليك تعديل ملف SQL يدوياً لنقل البيانات (مثلاً `RENAME COLUMN` بدلاً من حذفه، أو نقل البيانات لعمود جديد) قبل المتابعة.
+    *   بما أن التغييرات التي قمنا بها كانت في الغالب "إضافة" حقول (Add Columns) وجداول جديدة، فمن المفترض أن يكون الوضع آمناً.
+
+---
+
+## 🚀 المرحلة 4: إعداد أوامر التشغيل على Railway
+
+لضمان تطبيق التغييرات على قاعدة البيانات تلقائياً عند النشر:
+
+1.  اذهب إلى **Settings** في خدمة التطبيق على Railway.
+2.  تحقق من **Build Command**:
+    ```bash
+    npx prisma generate && next build
+    ```
+3.  تحقق من **Start Command** (أو Deploy Command):
+    من الضروري تشغيل `migrate deploy` هنا لتحديث قاعدة البيانات الإنتاجية:
+    ```bash
+    npx prisma migrate deploy && next start
+    ```
+
+---
+
+## 📡 المرحلة 5: الرفع والنشر (Deployment)
+
+الآن بعد التأكد من النسخ الاحتياطي ومتغيرات البيئة، يمكنك رفع الكود.
+
+1.  **رفع الكود إلى GitHub:**
+    ```bash
+    git add .
+    git commit -m "Major update: Drive integration, Schema changes, UI fixes"
+    git push origin main
+    ```
+
+2.  **مراقبة النشر:**
+    *   راقب الـ **Logs** في Railway أثناء عملية الـ Build والـ Deploy.
+    *   تأكد من ظهور رسالة تفيد بأن `Prisma migrations applied successfully`.
+
+---
+
+## ✅ المرحلة 6: التحقق بعد النشر
+
+بعد أن يصبح الموقع "Active":
+
+1.  سجل الدخول بحساب موجود مسبقاً (للتأكد من عدم ضياع المستخدمين).
+2.  جرب رفع ملف (لاختبار متغيرات Google Drive).
+3.  تصفح الدروس القديمة (للتأكد من سلامة البيانات).
+
+**ملاحظة:** إذا حدث أي خطأ كارثي، يمكنك استعادة النسخة الاحتياطية التي أنشأتها في المرحلة 1 فوراً.
+```

<!--
[PROMPT_SUGGESTION]كيف يمكنني كتابة سكربت SQL مخصص داخل ملف الـ migration لنقل البيانات من عمود قديم إلى جديد؟[/PROMPT_SUGGESTION]
[PROMPT_SUGGESTION]أريد إضافة صفحة "الصيانة" تظهر للمستخدمين أثناء عملية التحديث، كيف أفعل ذلك في Next.js؟[/PROMPT_SUGGESTION]
-->
