# خطة تطوير الألعاب التعليمية (Game Development Plan)

## الهدف
حصر صلاحية إضافة "الألعاب التعليمية" (مثل `game_division_machine`) بالمدير (Directeur) فقط، لضمان جودة المحتوى والتحكم التقني.

## التفاصيل التقنية

### 1. الواجهة الأمامية (Frontend)
- تعديل صفحة إنشاء/تعديل التمارين (`src/app/(main)/dashboard/.../exercises/create/page.tsx`).
- إخفاء خيار "لعبة" من قائمة أنواع التمارين (`Select`) للمعلمين والمشرفين.
- إظهار الخيار فقط عندما يكون المستخدم الحالي بصلاحية `directeur`.

### 2. الواجهة الخلفية (Backend API)
- إضافة شرط تحقق في `POST /api/exercises` و `PUT /api/exercises/[id]`.
- إذا كان `type` التمرين يبدأ بـ `game_`، يجب التحقق من أن `session.user.role === 'directeur'`.
- إرجاع خطأ `403 Forbidden` إذا حاول غير المدير إضافة لعبة.

### 3. سير العمل (Workflow)
- يستمر المعلمون في إنشاء الدروس والتمارين التقليدية.
- يتم إضافة الألعاب حصراً عن طريق حساب المدير بناءً على طلب المعلمين أو خطة المنهج.