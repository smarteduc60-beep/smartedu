# SmartEdu Platform 🎓

منصة تعليمية ذكية متكاملة تدعم أدوار متعددة مع نظام تقييم مدعوم بالذكاء الاصطناعي.

## 🚀 التقنيات المستخدمة

- **Frontend:** Next.js 15, React 19, TypeScript, Tailwind CSS, shadcn/ui
- **Backend:** Next.js API Routes
- **Database:** MySQL + Prisma ORM
- **Authentication:** NextAuth.js
- **AI:** Google Genkit (للتقييم الذكي)

## 📋 المتطلبات الأساسية

- Node.js 18+ 
- MySQL 8.0+
- npm أو yarn

## 🛠️ خطوات الإعداد

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd smartedu-Nodejs-main
```

### 2. تثبيت الحزم

```bash
npm install
```

### 3. إعداد قاعدة البيانات MySQL

قم بإنشاء قاعدة بيانات جديدة:

```sql
CREATE DATABASE smartedu CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. إعداد متغيرات البيئة

قم بتحديث ملف `.env` بمعلومات قاعدة البيانات الخاصة بك:

```env
DATABASE_URL="mysql://root:password@localhost:3306/smartedu"
NEXTAUTH_URL="http://localhost:9002"
NEXTAUTH_SECRET="your-super-secret-key-here"
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_GENAI_API_KEY="your-genai-api-key"
```

### 5. إنشاء جداول قاعدة البيانات

```bash
npm run db:push
```

### 6. ملء قاعدة البيانات بالبيانات الأولية

```bash
npm run db:seed
```

### 7. تشغيل المشروع

```bash
npm run dev
```

الآن افتح المتصفح على: [http://localhost:9002](http://localhost:9002)

## 🔑 بيانات الدخول التجريبية

بعد تشغيل `npm run db:seed`، يمكنك الدخول باستخدام:

| الدور | البريد الإلكتروني | كلمة المرور |
|------|-------------------|-------------|
| مدير | aisha.director@example.com | password123 |
| معلم | ahmed.teacher@example.com | password123 |
| طالب | fatima.student@example.com | password123 |
| ولي أمر | khalid.parent@example.com | password123 |

## 📦 الأوامر المتاحة

```bash
# تطوير
npm run dev                    # تشغيل بيئة التطوير
npm run build                  # بناء المشروع للإنتاج
npm run start                  # تشغيل النسخة المبنية

# قاعدة البيانات
npm run db:generate            # توليد Prisma Client
npm run db:push                # دفع Schema إلى قاعدة البيانات
npm run db:migrate             # إنشاء وتطبيق migrations
npm run db:seed                # ملء البيانات الأولية
npm run db:studio              # فتح Prisma Studio

# Genkit AI
npm run genkit:dev             # تشغيل Genkit Dev UI
npm run genkit:watch           # تشغيل Genkit مع المراقبة

# أخرى
npm run lint                   # فحص الكود
npm run typecheck              # فحص الأنواع
```

## 🏗️ هيكل المشروع

```
smartedu-Nodejs-main/
├── prisma/
│   ├── schema.prisma          # تعريف قاعدة البيانات
│   └── seed.ts                # بيانات أولية
├── src/
│   ├── app/                   # Next.js App Router
│   │   ├── (auth)/           # صفحات المصادقة
│   │   ├── (main)/           # صفحات التطبيق
│   │   └── api/              # API Routes
│   ├── components/           # مكونات React
│   ├── lib/                  # مكتبات مساعدة
│   │   ├── auth.ts          # إعدادات NextAuth
│   │   ├── prisma.ts        # Prisma Client
│   │   └── types.ts         # تعريفات الأنواع
│   ├── ai/                   # Genkit AI Flows
│   └── middleware.ts         # حماية المسارات
└── .env                      # متغيرات البيئة
```

## 🎯 الميزات الرئيسية

### ✅ نظام المصادقة والأدوار
- تسجيل دخول بالبريد الإلكتروني وكلمة المرور
- تسجيل دخول عبر Google OAuth
- 6 أدوار مختلفة: مدير، مشرف عام، مشرف مادة، معلم، طالب، ولي أمر
- حماية الصفحات حسب الأدوار

### 📚 إدارة المحتوى
- نظام هرمي: مراحل → مستويات → مواد
- إدارة الدروس والتمارين
- دروس عامة وخاصة
- رفع ملفات PDF وروابط فيديو

### 🤖 التقييم الذكي
- تقييم فوري للإجابات باستخدام Google Genkit
- تحليل الإجابات النصية والملفات
- ملاحظات مفصلة ودرجات
- مراجعة يدوية من المعلم

### 👥 إدارة المستخدمين
- ربط المعلمين بالطلاب
- ربط أولياء الأمور بالأبناء
- متابعة التقدم والأداء
- نظام رسائل داخلي

## 🔧 استكشاف الأخطاء

### خطأ في الاتصال بقاعدة البيانات

تأكد من:
1. تشغيل MySQL
2. صحة بيانات الاتصال في `.env`
3. إنشاء قاعدة البيانات `smartedu`

### خطأ في Prisma Client

قم بتوليد Client من جديد:

```bash
npm run db:generate
```

### خطأ في NextAuth

تأكد من:
1. وجود `NEXTAUTH_SECRET` في `.env`
2. صحة `NEXTAUTH_URL`

## 📝 الخطوات التالية

- [ ] إنشاء API Routes للعمليات الأساسية (CRUD)
- [ ] استبدال mock-data بـ API calls في الواجهات
- [ ] إضافة نظام رفع الملفات (Cloudinary أو S3)
- [ ] تفعيل نظام الرسائل الداخلي
- [ ] إضافة نظام الإشعارات
- [ ] تفعيل إدارة المستخدمين للمدير

## 📄 الترخيص

MIT License

## 👥 المطورون

SmartEdu Team
