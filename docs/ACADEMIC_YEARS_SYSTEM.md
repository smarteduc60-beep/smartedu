# 🎓 نظام السنوات الدراسية والترقيات التلقائية

## 📋 نظرة عامة

نظام شامل لإدارة انتقال الطلاب بين المستويات الدراسية في نهاية كل سنة دراسية (30 جوان) مع رسوم متحركة احترافية.

---

## ✨ المزايا الرئيسية

### 🎯 سير العمل الآلي

```
المدير → يُنشئ سنة دراسية → ينقر "إرسال رسائل" 
  ↓
النظام → يُرسل رسائل HTML منسقة لكل ولي أمر
  ↓
ولي الأمر → يرد "نعم" (نجح) أو "لا" (لم ينجح)
  ↓
النظام → يُرقي الطالب تلقائياً (تحديث levelId)
  ↓
الطالب → يرى رسالة متحركة (🎉 تهنئة أو 💪 تشجيع)
```

---

## 🗄️ قاعدة البيانات

### 1. جدول `academic_years`

```prisma
model AcademicYear {
  id         String   @id @default(cuid())
  name       String   // "2024-2025"
  startDate  DateTime // September 1st
  endDate    DateTime // June 30th
  isCurrent  Boolean  @default(false)
  status     String   @default("active") // active, ended, archived
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  
  promotions StudentPromotion[]
  
  @@index([isCurrent])
}
```

**الوصف**: يحتوي على معلومات السنة الدراسية (من 1 سبتمبر إلى 30 جوان).

---

### 2. جدول `student_promotions`

```prisma
model StudentPromotion {
  id              String    @id @default(cuid())
  academicYearId  String
  studentId       String
  parentId        String
  fromLevelId     Int
  toLevelId       Int?      // null إذا كان آخر مستوى في المرحلة
  status          String    @default("pending") // pending, approved, rejected, completed
  parentResponse  String?   // yes, no
  messageId       Int?      // رابط للرسالة المرسلة
  notifiedAt      DateTime? // تاريخ إرسال الرسالة
  respondedAt     DateTime? // تاريخ رد ولي الأمر
  promotedAt      DateTime? // تاريخ الترقية الفعلية
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  
  academicYear    AcademicYear @relation(fields: [academicYearId], references: [id], onDelete: Cascade)
  student         User         @relation("StudentPromotions", fields: [studentId], references: [id], onDelete: Cascade)
  parent          User         @relation("ParentPromotions", fields: [parentId], references: [id], onDelete: Cascade)
  fromLevel       Level        @relation("PromotionFromLevel", fields: [fromLevelId], references: [id])
  toLevel         Level?       @relation("PromotionToLevel", fields: [toLevelId], references: [id])
  
  @@unique([academicYearId, studentId])
  @@index([status])
  @@index([academicYearId])
}
```

**الوصف**: يتتبع عملية ترقية كل طالب مع جميع التفاصيل (الحالة، رد ولي الأمر، التواريخ).

---

## 🔌 APIs

### 1. `GET /api/academic-years`

**الوصف**: جلب قائمة السنوات الدراسية.

**Response**:
```json
{
  "years": [
    {
      "id": "clxy123...",
      "name": "2024-2025",
      "startDate": "2024-09-01T00:00:00.000Z",
      "endDate": "2025-06-30T23:59:59.999Z",
      "isCurrent": true,
      "status": "active",
      "_count": {
        "promotions": 150
      }
    }
  ]
}
```

---

### 2. `POST /api/academic-years`

**الوصف**: إنشاء سنة دراسية جديدة (للمدير فقط).

**Request Body**:
```json
{
  "name": "2025-2026",
  "startDate": "2025-09-01",
  "endDate": "2026-06-30",
  "isCurrent": true
}
```

**Logic**:
- يتحقق من صلاحيات المدير
- إذا `isCurrent = true`، يُلغي `isCurrent` لجميع السنوات الأخرى
- يُنشئ السنة الجديدة
- يُسجل في `logs`

---

### 3. `POST /api/academic-years/promotions/initiate`

**الوصف**: بدء عملية الترقية (إرسال رسائل لجميع أولياء الأمور).

**Request Body**:
```json
{
  "academicYearId": "clxy123..."
}
```

**Logic**:
1. جلب جميع روابط `parentChildLinks` حيث:
   - الطفل دوره `student`
   - الطفل لديه `levelId`
2. لكل طالب:
   - التحقق من عدم وجود ترقية سابقة
   - إيجاد المستوى التالي (`displayOrder + 1` في نفس المرحلة)
   - إنشاء `StudentPromotion` بحالة `pending`
   - إنشاء رسالة HTML منسقة لولي الأمر
   - تحديث `messageId` و `notifiedAt`
   - إنشاء إشعار لولي الأمر

**رسالة HTML للولي الأمر**:
```html
<div style="direction: rtl; text-align: right; padding: 20px;">
  <h2>السلام عليكم ورحمة الله وبركاته 🌟</h2>
  <p>عزيزي ولي الأمر، نرجو منكم الإجابة عن السؤال التالي:</p>
  
  <div style="background-color: #f5f5f5; padding: 15px; border-radius: 8px;">
    <p><strong>الطالب/ة:</strong> أحمد محمد</p>
    <p><strong>المستوى الحالي:</strong> السنة الأولى ابتدائي</p>
    <p><strong>المستوى التالي:</strong> السنة الثانية ابتدائي</p>
  </div>
  
  <p style="font-size: 18px; color: #2196F3; font-weight: bold;">
    🎓 هل نجح/ت أحمد في الانتقال إلى السنة الثانية ابتدائي؟
  </p>
  
  <p>يرجى الرد بـ:</p>
  <ul>
    <li><strong>"نعم"</strong> - إذا نجح الطالب وسينتقل للمستوى الأعلى</li>
    <li><strong>"لا"</strong> - إذا سيعيد نفس المستوى</li>
  </ul>
</div>
```

**Response**:
```json
{
  "success": true,
  "message": "تم إرسال 150 رسالة بنجاح",
  "messagesCreated": 150,
  "promotionsCreated": 150
}
```

---

### 4. `POST /api/academic-years/promotions/respond`

**الوصف**: معالجة رد ولي الأمر (yes/no).

**Request Body**:
```json
{
  "promotionId": "clxy456...",
  "response": "yes"  // or "no"
}
```

**Logic**:

#### إذا كان الرد "yes" (نجح):
1. تحديث `userDetails.levelId` إلى `promotion.toLevelId`
2. تحديث الترقية:
   - `status = 'completed'`
   - `parentResponse = 'yes'`
   - `respondedAt = now()`
   - `promotedAt = now()`
3. تسجيل في `logs`
4. إنشاء إشعار للطالب:
   - **Title**: 🎉 مبروك! تم ترقيتك
   - **Message**: تهانينا! تم ترقيتك إلى [اسم المستوى الجديد]

#### إذا كان الرد "no" (لم ينجح):
1. تحديث الترقية:
   - `status = 'rejected'`
   - `parentResponse = 'no'`
   - `respondedAt = now()`
2. إنشاء إشعار للطالب:
   - **Title**: 💪 لا تيأس!
   - **Message**: لا تقلق، ستكون السنة القادمة أفضل بإذن الله. استمر في التعلم والتحسن!

**Response**:
```json
{
  "success": true,
  "message": "تم ترقية الطالب بنجاح",
  "isApproved": true,
  "promoted": true
}
```

---

### 5. `GET /api/academic-years/promotions/stats?academicYearId=...`

**الوصف**: جلب إحصائيات الترقيات للمدير.

**Response**:
```json
{
  "stats": {
    "total": 150,
    "pending": 20,
    "approved": 120,
    "rejected": 10,
    "completed": 120,
    "responseRate": 86.67
  },
  "promotions": [
    {
      "id": "clxy456...",
      "status": "completed",
      "parentResponse": "yes",
      "notifiedAt": "2025-06-30T10:00:00.000Z",
      "respondedAt": "2025-07-01T08:30:00.000Z",
      "promotedAt": "2025-07-01T08:30:00.000Z",
      "student": {
        "id": "user123",
        "firstName": "أحمد",
        "lastName": "محمد",
        "email": "ahmed@example.com"
      },
      "parent": {
        "id": "user456",
        "firstName": "محمد",
        "lastName": "علي",
        "email": "parent@example.com"
      },
      "fromLevel": {
        "id": 1,
        "name": "السنة الأولى ابتدائي"
      },
      "toLevel": {
        "id": 2,
        "name": "السنة الثانية ابتدائي"
      }
    }
  ]
}
```

---

## 🎨 واجهات المستخدم

### 1. واجهة المدير - `/dashboard/directeur/promotions`

**المكونات**:

#### أ) اختيار السنة الدراسية
- بطاقات تعرض جميع السنوات
- Badge "الحالية" للسنة النشطة
- عدد الترقيات لكل سنة

#### ب) بطاقات الإحصائيات (5 بطاقات)
1. **إجمالي الطلاب**: العدد الكلي
2. **في الانتظار**: عدد الردود المعلقة (أصفر)
3. **نجح**: عدد الموافقات (أخضر)
4. **لم ينجح**: عدد الرفض (أحمر)
5. **معدل الاستجابة**: نسبة مئوية (أزرق)

#### ج) زر بدء الترقية
- يظهر فقط إذا لم يتم إرسال رسائل بعد
- عند النقر: Alert تأكيد
- بعد التأكيد: Confetti ورسالة نجاح

#### د) جدول الترقيات
- الأعمدة: الطالب، ولي الأمر، المستوى الحالي، المستوى التالي، الحالة، تاريخ الإرسال، تاريخ الرد
- Badges ملونة للحالات:
  - `pending` → رمادي (Clock)
  - `approved` → أخضر (CheckCircle)
  - `rejected` → أحمر (XCircle)
  - `completed` → أزرق (GraduationCap)

---

### 2. واجهة ولي الأمر - `<PromotionResponse>`

**المكون**: يُستخدم في صفحة الرسائل عند عرض رسالة ترقية.

#### الحالات:

##### أ) قبل الرد:
- بطاقة مُنسقة بتدرج لوني
- معلومات الطالب والمستويات
- سؤال واضح: "هل نجح/ت ... في الانتقال؟"
- زرين كبيرين:
  - **نعم، نجح ✓** (أخضر)
  - **لا، لم ينجح** (رمادي)
- Alert تأكيد قبل الإرسال

##### ب) بعد الرد "نعم" (رسوم متحركة):
```
🎊 Confetti متساقط (500 قطعة)
┌─────────────────────────────────────┐
│                                     │
│        [دائرة خضراء متحركة]        │
│              ✓                      │
│                                     │
│      🎉 ألف مبروك! 🎉              │
│                                     │
│    تم ترقية أحمد محمد              │
│                                     │
│   السنة الأولى ابتدائي              │
│          ↓                          │
│   السنة الثانية ابتدائي             │
│                                     │
│   🏆 🎓 ⭐ 🌟 ✨                    │
│                                     │
│  تهانينا على هذا الإنجاز الرائع!   │
│  نتمنى لكم التوفيق والنجاح...      │
│                                     │
│      [زر: شكراً لكم! 💚]           │
│                                     │
└─────────────────────────────────────┘
```

**التأثيرات**:
- Confetti من مكتبة `react-confetti`
- Animations:
  - `animate-ping` للخلفية
  - `animate-bounce` للدائرة
  - `animate-in zoom-in` للعناصر
  - `slide-in-from-bottom` للنصوص
- تدرج لوني: أخضر → أزرق فاتح

##### ج) بعد الرد "لا" (رسوم متحركة):
```
┌─────────────────────────────────────┐
│                                     │
│        [دائرة زرقاء متحركة]        │
│              💪                     │
│                                     │
│            لا تيأس!                 │
│                                     │
│          أحمد محمد                  │
│                                     │
│   سيواصل في السنة الأولى ابتدائي    │
│                                     │
│   🌅 📚 🎯 ⚡ 💫                    │
│                                     │
│         لا تقلق!                    │
│  ستكون السنة القادمة أفضل بإذن الله │
│                                     │
│  استمر في التعلم والتحسن،           │
│    والنجاح في انتظارك!              │
│                                     │
│  تذكر: كل تجربة فرصة للتعلم 🌱      │
│                                     │
│  [زر: نعم، سنحاول مرة أخرى! 💙]    │
│                                     │
└─────────────────────────────────────┘
```

**التأثيرات**:
- Animations:
  - `animate-pulse` للخلفية
  - `animate-bounce` للدائرة
  - `animate-in zoom-in` للعناصر
  - `slide-in-from-bottom` للنصوص
- تدرج لوني: أزرق → سماوي فاتح
- نص تحفيزي ومُشجع

---

## 📦 التبعيات الجديدة

```json
{
  "react-confetti": "^6.1.0",
  "date-fns": "^2.30.0"
}
```

---

## 🧩 الملفات المُنشأة/المُعدلة

### 1. الملفات الجديدة:

| الملف | السطور | الوصف |
|-------|--------|-------|
| `src/app/api/academic-years/route.ts` | 110 | CRUD للسنوات الدراسية |
| `src/app/api/academic-years/promotions/initiate/route.ts` | 184 | إرسال رسائل الاستفسار |
| `src/app/api/academic-years/promotions/respond/route.ts` | 135 | معالجة رد ولي الأمر |
| `src/app/api/academic-years/promotions/stats/route.ts` | 88 | إحصائيات الترقيات |
| `src/app/(main)/dashboard/directeur/promotions/page.tsx` | 460+ | واجهة المدير |
| `src/components/PromotionResponse.tsx` | 300+ | واجهة ولي الأمر |
| `src/hooks/use-window-size.tsx` | 25 | Hook للأبعاد |

### 2. الملفات المُعدلة:

| الملف | التغييرات |
|-------|-----------|
| `prisma/schema.prisma` | إضافة `AcademicYear`, `StudentPromotion`, علاقات في `User` و `Level` |
| `README.md` | تحديث النسبة لـ 100%، إضافة قسم السنوات الدراسية |

---

## 🔍 الحالات الخاصة

### 1. طالب في آخر مستوى بالمرحلة
- `toLevelId = null`
- يظهر Badge "نهاية المرحلة" في الجدول
- لا يتم ترقيته (يبقى في نفس المستوى)

### 2. عدم وجود ولي أمر مرتبط
- الطالب لن يظهر في قائمة الترقيات
- يمكن ربطه لاحقاً

### 3. تكرار عملية الترقية
- يتحقق API من وجود ترقية سابقة
- يتخطى الطلاب المُسجلين مسبقاً

### 4. إلغاء السنة الحالية
- عند تعيين سنة جديدة كـ `isCurrent`
- يتم إلغاء `isCurrent` لجميع السنوات الأخرى تلقائياً

---

## 📊 مثال على سير العمل الكامل

### السيناريو:
المدرسة لديها 150 طالب، نهاية السنة الدراسية 2024-2025.

### الخطوات:

1. **1 سبتمبر 2024**: المدير يُنشئ السنة الدراسية
   ```
   Name: "2024-2025"
   Start: 2024-09-01
   End: 2025-06-30
   isCurrent: true
   ```

2. **30 جوان 2025**: نهاية السنة، المدير ينقر "إرسال رسائل الاستفسار"
   - النظام يُنشئ 150 `StudentPromotion` بحالة `pending`
   - يُرسل 150 رسالة HTML لأولياء الأمور
   - يُنشئ 150 إشعار

3. **1-10 جويلية 2025**: أولياء الأمور يردون
   - 120 يردون "نعم" → الطلاب يُرقون تلقائياً
   - 10 يردون "لا" → الطلاب يُعيدون نفس المستوى
   - 20 لم يردوا بعد

4. **إحصائيات المدير**:
   ```
   Total: 150
   Pending: 20 (13%)
   Approved: 120 (80%)
   Rejected: 10 (7%)
   Response Rate: 86.67%
   ```

5. **الطلاب**:
   - 120 طالب يرون رسالة تهنئة متحركة 🎉
   - 10 طلاب يرون رسالة تشجيع متحركة 💪

---

## 🎯 الفوائد

✅ **توفير الوقت**: إرسال 150 رسالة بنقرة واحدة  
✅ **تلقائي 100%**: الترقيات تتم دون تدخل يدوي  
✅ **تجربة مستخدم رائعة**: رسوم متحركة احترافية  
✅ **شفافية**: إحصائيات كاملة للمدير  
✅ **تواصل واضح**: رسائل مُنسقة بالـ HTML  
✅ **تحفيز**: رسائل تشجيع للطلاب المعيدين  
✅ **احتفال**: رسوم متحركة للطلاب الناجحين  

---

## 🚀 التطورات المستقبلية (اختياري)

- [ ] تصدير تقرير PDF بالترقيات
- [ ] إشعار بريد إلكتروني لأولياء الأمور
- [ ] رسوم بيانية للإحصائيات
- [ ] سجل تاريخي لترقيات كل طالب
- [ ] تذكيرات تلقائية لأولياء الأمور المتأخرين
- [ ] دعم الترقية الجماعية اليدوية (في حالات خاصة)

---

## ✅ الخلاصة

النظام الآن يدعم **دورة كاملة** لانتقال الطلاب بين المستويات مع:
- 🎓 إدارة السنوات الدراسية
- 📨 إرسال رسائل آلي
- 🔄 ترقية تلقائية
- 🎉 رسوم متحركة احترافية
- 📊 إحصائيات شاملة

**النتيجة**: منصة متكاملة 100% لإدارة العملية التعليمية من الألف إلى الياء! 🎯
