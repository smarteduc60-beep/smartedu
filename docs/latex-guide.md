# دليل استخدام الصيغ الرياضية (LaTeX)

## كيفية كتابة الصيغ الرياضية في التمارين

عند إنشاء تمرين، يمكنك استخدام صيغ LaTeX لعرض المعادلات الرياضية بشكل جميل.

### 1. الصيغ في السطر (Inline Math)

للصيغ البسيطة التي تظهر داخل السطر، استخدم `\(` و `\)`:

```
هذه معادلة بسيطة: \(x = 5\)
المعادلة التربيعية: \(x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}\)
```

**النتيجة:**
هذه معادلة بسيطة: \(x = 5\)
المعادلة التربيعية: \(x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}\)

### 2. الصيغ المعروضة (Display Math)

للمعادلات الكبيرة التي تظهر في الوسط، استخدم `$$` في بداية ونهاية المعادلة:

```
$$E = mc^2$$

$$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$$
```

**النتيجة:**
$$E = mc^2$$

$$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$$

## أمثلة شائعة

### كسور
```
\(\frac{1}{2}\) - نصف
\(\frac{a + b}{c}\) - كسر مع جمع
```

### جذور
```
\(\sqrt{16} = 4\) - جذر تربيعي
\(\sqrt[3]{27} = 3\) - جذر تكعيبي
```

### أسس (Powers)
```
\(x^2\) - تربيع
\(2^{10} = 1024\) - أس كبير
```

### مجاميع وتكاملات
```
$$\sum_{i=1}^{n} x_i$$
$$\int_{0}^{\infty} e^{-x} dx$$
```

### رموز يونانية
```
\(\alpha, \beta, \gamma, \Delta, \theta, \pi\)
```

### معادلات متعددة الأسطر
```
$$\begin{aligned}
x + y &= 5 \\
2x - y &= 1
\end{aligned}$$
```

## نصائح مهمة

1. **استخدام الأقواس**: استخدم `\left(` و `\right)` للأقواس الكبيرة:
   ```
   \(\left(\frac{a}{b}\right)^2\)
   ```

2. **المسافات**: استخدم `\,` للمسافات الصغيرة و `\quad` للمسافات الكبيرة

3. **النص في المعادلات**: استخدم `\text{}`:
   ```
   \(x = 5 \text{ متر}\)
   ```

4. **الأقواس المجموعة**: استخدم `\{` و `\}` للأقواس المجموعة:
   ```
   \(\{x | x > 0\}\)
   ```

## الذكاء الصناعي وLaTeX

عند استخدام زر "توليد بالذكاء الصناعي" في صفحة إنشاء التمارين:

✅ **الذكاء الصناعي سيقوم تلقائيًا بـ:**
- إضافة صيغ LaTeX للمعادلات الرياضية
- استخدام `\(` و `\)` للصيغ في السطر
- استخدام `$$` للمعادلات المعروضة
- تنسيق الإجابة بشكل احترافي

📝 **مثال على إجابة مُولدة:**

**السؤال:** ما هو حل المعادلة \(x^2 - 5x + 6 = 0\)؟

**الإجابة المُولدة:**
```
لحل المعادلة التربيعية \(x^2 - 5x + 6 = 0\):

نستخدم الصيغة العامة:
$$x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}$$

حيث \(a = 1\), \(b = -5\), \(c = 6\)

الحل:
\(x_1 = 3\) أو \(x_2 = 2\)
```

## اختبار الصيغ

يمكنك اختبار الصيغ الرياضية على صفحة الاختبار:
`/test-math`

## مراجع إضافية

- [KaTeX Documentation](https://katex.org/docs/supported.html)
- [LaTeX Math Symbols](https://www.overleaf.com/learn/latex/List_of_Greek_letters_and_math_symbols)
