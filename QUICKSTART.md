# 🚀 دليل البدء السريع - SmartEdu Platform

## ⚡ خطوات الإعداد السريعة (5 دقائق)

### 1️⃣ تثبيت المتطلبات

```bash
# تأكد من تثبيت Node.js 18+
node --version

# تأكد من تشغيل MySQL
mysql --version
```

### 2️⃣ إعداد قاعدة البيانات

```bash
# افتح MySQL
mysql -u root -p

# أنشئ قاعدة البيانات
CREATE DATABASE smartedu CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

### 3️⃣ إعداد المشروع

```bash
# انسخ ملف البيئة
cp .env.example .env

# عدّل .env وضع معلومات قاعدة البيانات الخاصة بك
# DATABASE_URL="mysql://root:كلمة_المرور@localhost:3306/smartedu"

# ثبت الحزم
npm install

# أنشئ جداول قاعدة البيانات
npm run db:push

# املأ البيانات الأولية
npm run db:seed

# شغّل المشروع
npm run dev
```

### 4️⃣ افتح المتصفح

انتقل إلى: **http://localhost:9002**

---

## 🔑 بيانات الدخول

| الدور | البريد | كلمة المرور |
|------|---------|-------------|
| مدير | aisha.director@example.com | password123 |
| معلم | ahmed.teacher@example.com | password123 |
| طالب | fatima.student@example.com | password123 |
| ولي أمر | khalid.parent@example.com | password123 |

---

## 🛠️ أوامر مفيدة

```bash
# التطوير
npm run dev              # تشغيل المشروع
npm run db:studio        # فتح واجهة قاعدة البيانات

# إعادة تعيين قاعدة البيانات (للتطوير فقط!)
npx prisma db push --force-reset
npm run db:seed

# عرض السجلات
npm run dev              # سترى سجلات Prisma في الـ console
```

---

## ❓ مشاكل شائعة

### لا يمكن الاتصال بقاعدة البيانات؟

✅ تأكد من تشغيل MySQL  
✅ تأكد من صحة `DATABASE_URL` في `.env`  
✅ تأكد من إنشاء قاعدة البيانات `smartedu`

### خطأ "Prisma Client not found"؟

```bash
npm run db:generate
```

### خطأ في تسجيل الدخول؟

✅ تأكد من تنفيذ `npm run db:seed`  
✅ تأكد من وجود `NEXTAUTH_SECRET` في `.env`

---

## 📚 مزيد من المعلومات

- **دليل الإعداد الكامل:** راجع `SETUP.md`
- **سجل التطوير:** راجع `DEVELOPMENT_LOG.md`
- **وثائق Prisma:** https://www.prisma.io/docs
- **وثائق NextAuth:** https://next-auth.js.org

---

## 🎯 الخطوة التالية

بعد تشغيل المشروع:
1. جرّب تسجيل الدخول بالحسابات التجريبية
2. استكشف لوحات التحكم المختلفة
3. تحقق من نظام التقييم الذكي للطلاب

**استمتع بالتطوير! 🚀**
