# SmartEdu Platform 🎓

منصة تعليمية ذكية متكاملة تهدف إلى تسهيل إدارة العملية التعليمية، ربط المعلمين بالطلاب، وتوظيف الذكاء الاصطناعي لتحسين جودة التعليم.

## 🚀 التقنيات المستخدمة (Tech Stack)

- **Frontend:** Next.js 15 (App Router), React 19, TypeScript 5, Tailwind CSS, shadcn/ui.
- **Backend:** Next.js API Routes, Node.js 18+.
- **Database:** Prisma ORM v6 (MySQL 8.0+).
- **Authentication:** NextAuth.js v4 (Credentials + Google OAuth).
- **Storage:** Google Drive API (Custom Integration with Proxy & Caching).
- **AI:** DeepSeek & Google Gemini (via Genkit) for automated grading and content generation.
- **Visualization:** JSXGraph for interactive geometry.

## ✨ المميزات الرئيسية

### 🔐 الأمان والصلاحيات
- نظام تسجيل دخول آمن متعدد الأدوار (مدير، مشرف، معلم، طالب، ولي أمر).
- حماية المسارات (Middleware) والتحقق من الصلاحيات في الـ API.

### 📂 إدارة المحتوى والملفات
- **Google Drive Integration:** رفع الملفات وتنظيمها تلقائياً في مجلدات هرمية (مرحلة > مستوى > مادة).
- **Smart Proxy:** نظام وكيل ذكي لعرض الصور المحمية من Google Drive دون مشاكل (403 Forbidden).
- **Rich Content:** محرر نصوص متطور (Tiptap) يدعم المعادلات الرياضية (LaTeX/KaTeX) والوسائط.

### 📐 الرسوم الهندسية التفاعلية
- **Interactive Geometry:** إنشاء وعرض رسوم هندسية تفاعلية باستخدام JSXGraph.
- **AI Generation:** توليد أوامر الرسم الهندسي تلقائياً من نص السؤال باستخدام الذكاء الاصطناعي.

### 🤖 الذكاء الاصطناعي
- **التصحيح التلقائي:** تقييم إجابات الطلاب ومقارنتها بالإجابة النموذجية.
- **التغذية الراجعة:** تقديم ملاحظات فورية ونقاط القوة والضعف للطالب.
- **توليد المحتوى:** مساعدة المعلمين في إنشاء إجابات نموذجية ورسوم توضيحية.

## 🛠️ متطلبات التشغيل (Prerequisites)

- **Node.js:** الإصدار 18 أو أحدث.
- **Database:** MySQL 8.0 أو أحدث.

## ⚙️ الإعداد والتشغيل (Installation & Setup)

1. **تثبيت الاعتماديات:**
   ```bash
   npm install
   ```

2. **إعداد متغيرات البيئة (.env):**
   قم بإنشاء ملف `.env` في المجلد الرئيسي وأضف المتغيرات التالية:

   ```env
   # Database
   DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"

   # NextAuth
   NEXTAUTH_URL="http://localhost:3000"
   NEXTAUTH_SECRET="your-super-secret-key"

   # Google Drive Service Account (Storage)
   GOOGLE_CLIENT_EMAIL="your-service-account-email"
   GOOGLE_PRIVATE_KEY="your-private-key"
   GOOGLE_DRIVE_ROOT_FOLDER_ID="your-root-folder-id"

   # AI Integration
   DEEPSEEK_API_KEY="your-deepseek-api-key"
   GOOGLE_GENAI_API_KEY="your-gemini-api-key" # Required for Genkit flows
   ```

3. **تهيئة قاعدة البيانات:**
   ```bash
   npx prisma generate
   npx prisma db push
   ```

4. **تشغيل السيرفر:**
   ```bash
   npm run dev
   ```

## 📚 التوثيق (Documentation)

- **هيكلية المشروع:** راجع ملف `structure.md` لفهم توزيع الملفات والمسارات.
- **خطة العمل:** راجع ملف `plan.md` لمتابعة تقدم التطوير والميزات المستقبلية.

## 📊 حالة المشروع

- **الإصدار:** 0.2.0
- **حالة التطوير:** Active Development / Beta
- **نسبة الإنجاز:** ~90%

## 📝 الملاحظات الأخيرة
- تم إضافة نظام الرسوم الهندسية التفاعلية.
- تم تحسين تكامل الذكاء الاصطناعي ليشمل DeepSeek و Gemini.
- تم توثيق هيكلية المشروع بالكامل في `structure.md`.

---

Developed with ❤️ by SmartEdu Team.
