# 🚀 دليل سريع: رفع المشروع إلى GitHub

## ⚡ الطريقة السريعة (3 خطوات)

```powershell
# 1. فحص الأمان
.\check-security.ps1

# 2. الرفع التلقائي
.\push-to-github.ps1 -message "وصف التحديث"

# 3. تم! ✅
```

---

## 📋 الطريقة اليدوية

### 1️⃣ أول مرة فقط - الإعداد الأولي

```powershell
# انتقل للمجلد
cd C:\Users\pc\Desktop\smartedu-Nodejs-main

# إعداد معلومات Git
git config user.name "اسمك"
git config user.email "your-email@example.com"

# أنشئ Repository على GitHub أولاً:
# https://github.com/new

# ربط المشروع مع GitHub (استبدل USERNAME واسم المشروع)
git remote add origin https://github.com/USERNAME/smartedu-platform.git

# أول رفع
git add .
git commit -m "🎓 Initial commit: SmartEdu Platform v1.0"
git push -u origin master
```

### 2️⃣ التحديثات اللاحقة

```powershell
# فحص التغييرات
git status

# فحص الأمان (موصى به)
.\check-security.ps1

# إضافة وحفظ
git add .
git commit -m "وصف التحديث"
git push
```

---

## 🔐 ملفات محمية تلقائياً

هذه الملفات **لن يتم رفعها** (محمية بـ `.gitignore`):

- ✅ `.env` - متغيرات البيئة
- ✅ `node_modules/` - المكتبات
- ✅ `.next/` - ملفات Next.js
- ✅ `*.sql` - نسخ احتياطية
- ✅ `backups/` - مجلد النسخ
- ✅ `certificates/` - شهادات SSL
- ✅ `*.zip` - ملفات مضغوطة

---

## 💡 نصائح سريعة

### ✅ افعل:
- ✅ افحص الأمان قبل كل رفع: `.\check-security.ps1`
- ✅ اكتب رسالة commit وصفية
- ✅ استخدم `.env.example` للقيم النموذجية
- ✅ اجعل Repository خاصاً (Private) في البداية

### ❌ لا تفعل:
- ❌ لا ترفع ملف `.env` أبداً
- ❌ لا ترفع ملفات SQL
- ❌ لا تضع كلمات سر حقيقية في الكود
- ❌ لا ترفع مجلد `node_modules/`

---

## 🆘 حل المشاكل الشائعة

### مشكلة: "failed to push"
```powershell
# تحديث المشروع أولاً
git pull origin master
# ثم حاول مجدداً
git push
```

### مشكلة: رفعت .env بالخطأ!
```powershell
# احذفه من Git
git rm --cached .env
git commit -m "Remove .env"
git push

# ⚠️ غيّر جميع كلمات السر فوراً!
```

### مشكلة: تعارض في الملفات
```powershell
# اسحب التغييرات
git pull origin master
# حل التعارضات يدوياً في الملفات
git add .
git commit -m "Resolve conflicts"
git push
```

---

## 📚 أوامر Git مفيدة

```powershell
# حالة المشروع
git status

# عرض السجل
git log --oneline

# التراجع عن آخر commit (قبل push)
git reset HEAD~1

# عرض الفروع
git branch

# إنشاء فرع جديد
git checkout -b feature-name

# التبديل بين الفروع
git checkout master

# دمج فرع
git merge feature-name

# عرض الـ Remote
git remote -v

# تحديث من GitHub
git pull

# عرض الفروق
git diff
```

---

## 🎯 رسائل Commit جيدة

استخدم رموز تعبيرية للوضوح:

```
✨ Add: ميزة جديدة
🐛 Fix: إصلاح خطأ
📝 Docs: تحديث التوثيق
🎨 Style: تحسين التصميم
♻️ Refactor: إعادة هيكلة
🔐 Security: تحسين الأمان
⚡ Perf: تحسين الأداء
🚀 Deploy: نشر إصدار جديد
```

أمثلة:
```powershell
git commit -m "✨ Add: نظام الإشعارات الفوري"
git commit -m "🐛 Fix: مشكلة تسجيل الدخول"
git commit -m "📝 Docs: تحديث دليل التثبيت"
```

---

## 📞 روابط مفيدة

- 🌐 إنشاء Repository: https://github.com/new
- 📖 توثيق Git: https://git-scm.com/doc
- 💡 دليل GitHub: https://docs.github.com
- 🔐 إدارة SSH Keys: https://docs.github.com/en/authentication

---

**آخر تحديث**: 13 ديسمبر 2025
